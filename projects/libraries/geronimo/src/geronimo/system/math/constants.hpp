
#pragma once

#include <cmath>

namespace gero {
namespace math {
constexpr float pi = float(M_PI);
constexpr float pi2 = pi * 2.0f;
constexpr float hpi = pi * 0.5f;  // h = half
constexpr float qpi = hpi * 0.5f; // q = quart
constexpr float opi = qpi * 0.5f; // o = octo
} // namespace math
} // namespace gero
